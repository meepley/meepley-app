import React from "react";
import { Flex, Icon, Text } from "native-base";

const IconText = ({ text, iconLibrary, iconName }) => {
  return (
    <Flex>
      <Icon />
      <Text></Text>
    </Flex>
  );
};

export default IconText;
